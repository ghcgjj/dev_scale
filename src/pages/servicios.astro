---
import Layout from '../layouts/Layout.astro';
import Navbar from '../components/Navbar.astro';

const servicios = [
  {
    id: "01",
    titulo: "Landing Page",
    sub: "Alta Conversión",
    precio: "199€",
    tachado: "399€",
    color: "red",
    detalles: ["Diseño UX/UI Único", "Optimización de Carga", "Mobile First Architecture"],
    extras: [
        { nombre: "SEO Google (Posicionamiento)", precio: "+99€" },
        { nombre: "Copywriting Persuasivo", precio: "+49€" }
    ]
  },
  {
    id: "02",
    titulo: "Web Negocio",
    sub: "Autoridad Digital",
    precio: "499€",
    tachado: "950€",
    color: "gold",
    destacado: true,
    detalles: ["Hasta 5 Secciones Pro", "Gestor de Contenidos", "Seguridad Blindada"],
    extras: [
        { nombre: "SEO Local (Google Maps)", precio: "+150€" },
        { nombre: "Mantenimiento Élite", precio: "+29€/mes" }
    ]
  },
  {
    id: "03",
    titulo: "E-Commerce",
    sub: "Venta Global",
    precio: "899€",
    tachado: "1.800€",
    color: "red",
    detalles: ["Pasarela de Pagos Segura", "Gestión de Inventario", "Automatización de Pedidos"],
    extras: [
        { nombre: "SEO para Productos", precio: "+200€" },
        { nombre: "Integración con RRSS", precio: "+75€" }
    ]
  }
];
---

<Layout title="Servicios Élite | dev.scale">
    <Navbar />
    <main class="bg-grid pt-48 pb-32 px-6 max-w-7xl mx-auto">
        
        <header class="text-center mb-32" data-aos="zoom-in">
            <span class="text-red-600 font-black tracking-[1em] uppercase text-[10px] block mb-4">Pricing Strategies</span>
            <h2 class="text-6xl md:text-8xl font-black italic uppercase tracking-tighter leading-none">
                PLANES DE <br/> <span class="text-stroke">ESCALADO</span>
            </h2>
            <div class="mt-10 inline-flex items-center gap-4 glass px-6 py-2 rounded-full border-red-600/30">
                <span class="w-2 h-2 bg-red-600 rounded-full animate-ping"></span>
                <p class="text-[10px] font-bold tracking-[0.2em] uppercase">Disponibilidad: 3 Cupos mensuales</p>
            </div>
        </header>

        <div class="grid lg:grid-cols-3 gap-8">
            {servicios.map((s, i) => (
                <div 
                    class={`relative glass p-10 rounded-3xl flex flex-col transition-all duration-500 hover:-translate-y-4 ${s.destacado ? 'border-[#d4af37]/40 shadow-[0_0_50px_rgba(212,175,55,0.1)] scale-105 z-10' : 'hover:border-red-600/40'}`}
                    data-aos="fade-up" 
                    data-aos-delay={i * 200}
                >
                    {s.destacado && (
                        <div class="absolute -top-4 left-1/2 -translate-x-1/2 bg-[#d4af37] text-black text-[10px] font-black px-6 py-1 rounded-full uppercase">
                            Más Demandado
                        </div>
                    )}

                    <span class="text-4xl font-black opacity-10 font-mono mb-4">{s.id}</span>
                    <h3 class="text-3xl font-black italic uppercase tracking-tighter mb-1">{s.titulo}</h3>
                    <p class="text-[#d4af37] text-[10px] font-bold tracking-[0.3em] uppercase mb-8">{s.sub}</p>

                    <div class="mb-10">
                        <span class="text-5xl font-black tracking-tighter">{s.precio}</span>
                        <span class="text-zinc-600 line-through ml-2 text-xl font-light">{s.tachado}</span>
                    </div>

                    <ul class="space-y-4 mb-12 flex-grow">
                        {s.detalles.map(item => (
                            <li class="text-zinc-400 text-xs flex items-center gap-3">
                                <span class="w-1 h-1 bg-red-600 rounded-full"></span> {item}
                            </li>
                        ))}
                    </ul>

                    {/* Sección de Extras Opcionales */}
                    <div class="border-t border-white/5 pt-8 mb-10">
                        <p class="text-[9px] font-black text-zinc-500 uppercase tracking-[0.2em] mb-6">Mejoras Disponibles:</p>
                        <div class="space-y-4">
                            {s.extras.map(extra => (
                                <div class="flex justify-between items-center group/extra cursor-pointer">
                                    <span class="text-[11px] text-zinc-400 group-hover/extra:text-white transition-colors uppercase italic tracking-tighter">{extra.nombre}</span>
                                    <span class="text-[11px] font-bold text-[#d4af37]">{extra.precio}</span>
                                </div>
                            ))}
                        </div>
                    </div>

                    <a href="/contacto" class={`w-full py-5 rounded-xl font-black uppercase italic tracking-widest transition-all text-center ${s.destacado ? 'bg-[#d4af37] text-black hover:bg-white' : 'bg-red-600 text-white hover:bg-white hover:text-black'}`}>
                        Iniciar Proyecto
                    </a>
                </div>
            ))}
        </div>

        {/* Garantía de Calidad */}
        <section class="mt-32 border border-white/5 rounded-3xl p-12 glass flex flex-col md:flex-row justify-between items-center gap-8" data-aos="fade-in">
            <div class="flex gap-10">
                <div>
                    <p class="text-[10px] font-bold text-red-600 uppercase mb-2">Infraestructura</p>
                    <p class="text-2xl font-black italic">100% CLEAN CODE</p>
                </div>
                <div>
                    <p class="text-[10px] font-bold text-[#d4af37] uppercase mb-2">Rendimiento</p>
                    <p class="text-2xl font-black italic">99/100 SPEED</p>
                </div>
            </div>
            <p class="text-zinc-500 text-xs max-w-sm font-mono leading-relaxed">
                Cada línea de código es escrita a mano para garantizar que tu marca sea la más rápida de su sector. Sin plantillas pesadas. Sin basura digital.
            </p>
        </section>
    </main>
</Layout>