---
import Layout from '../layouts/Layout.astro';
import Navbar from '../components/Navbar.astro';
---
<Layout title="Contacto">
    <Navbar />
    <main class="pt-48 pb-20 px-10 max-w-4xl mx-auto">
        <h1 class="text-6xl font-black mb-8 italic italic uppercase">Hablemos</h1>
        <p class="text-zinc-500 text-xl mb-16">Estás a un paso de escalar tu infraestructura digital.</p>

        <form action="https://formspree.io/f/mnjqvvna" method="POST" class="grid md:grid-cols-2 gap-8">
            <div class="flex flex-col gap-2">
                <label class="text-[10px] uppercase font-bold text-zinc-600 tracking-widest">Nombre</label>
                <input type="text" name="name" required class="bg-transparent border-b border-white/10 py-4 outline-none focus:border-blue-600 transition" placeholder="Tu nombre">
            </div>
            <div class="flex flex-col gap-2">
                <label class="text-[10px] uppercase font-bold text-zinc-600 tracking-widest">Email</label>
                <input type="email" name="email" required class="bg-transparent border-b border-white/10 py-4 outline-none focus:border-blue-600 transition" placeholder="email@empresa.com">
            </div>
            <div class="md:col-span-2 flex flex-col gap-2">
                <label class="text-[10px] uppercase font-bold text-zinc-600 tracking-widest">Proyecto</label>
                <textarea name="message" required class="bg-transparent border-b border-white/10 py-4 outline-none focus:border-blue-600 transition h-32" placeholder="Cuéntanos qué necesitas..."></textarea>
            </div>
            <button type="submit" class="md:col-span-2 bg-white text-black font-black py-6 rounded-2xl hover:bg-blue-600 hover:text-white transition-all text-xl uppercase tracking-tighter">
                Enviar solicitud →
            </button>
        </form>
    </main>
</Layout>